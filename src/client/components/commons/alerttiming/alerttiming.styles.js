import { css } from '@emotion/core';

const styles = {
    formContainer: css`
        display: flex;
        flex: 1;
        justify-content: center;
        align-items: center;
    `,
};

export default styles;
